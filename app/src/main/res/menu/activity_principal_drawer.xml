<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/pguia"
            android:icon="@drawable/ic_menu_send"
            android:title="Guías" />
        <item
            android:id="@+id/psimulador"
            android:icon="@drawable/ic_menu_send"
            android:title="Simulador" />
        <item
            android:id="@+id/potra"
            android:icon="@drawable/ic_menu_send"
            android:title="Otra Opción" />
    </group>

    <item android:title="Herramientas">
        <menu>
            <item
                android:id="@+id/psalir"
                android:icon="@drawable/ic_menu_send"
                android:title="Salir" />
            <item
                android:id="@+id/padsep"
                android:icon="@drawable/ic_menu_send"
                android:title="ADSEP" />
        </menu>
    </item>

</menu>
